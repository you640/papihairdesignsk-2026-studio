// This file is intentionally left blank in the source.
// The service worker logic will be dynamically generated by Next.js via the next-pwa plugin.
// For Firebase Messaging, we need to import and initialize it.

import { initializeApp } from 'firebase/app';
import { getMessaging } from 'firebase/messaging/sw';

// This configuration will be replaced by environment variables during the build process
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
  measurementId: "YOUR_MEASUREMENT_ID"
};

const app = initializeApp(firebaseConfig);
const messaging = getMessaging(app);
